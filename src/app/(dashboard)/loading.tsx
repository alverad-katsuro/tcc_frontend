"use client";
import { Spinner } from "@/components/flowbite-components";

export default function Loading() {

    return (
        <Spinner
            aria-label="Extra large spinner example"
            size="xl"
        />
    )
}